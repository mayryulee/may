[build]
  command = "npm run build"
  publish = "dist"

# Netlify 기본 URL(boisterous-xxx) 대신 실제 공유 도메인 사용
[build.environment]
  SITE_URL = "https://formayletter.netlify.app"
  VITE_SITE_URL = "https://formayletter.netlify.app"
